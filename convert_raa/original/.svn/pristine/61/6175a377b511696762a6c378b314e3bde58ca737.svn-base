# ING RAA package / Zope Product

import monkey # fix ZopeCursor.query behaviour

def initialize(context):
    
    context.registerClass(
        container.Raa, constructors = (
            container.manage_addRaaForm, 
            container.manage_addRaa),
            )
